

server:
  port: 8181

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: pass

mybatis:
  type-aliases-package: classpath:com.spj.myabtis.dao


logging:
  level:
    com.spj.mybatis.dao: debug
